coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: 60%
        threshold: 10%
        informational: true

ignore:
  - "crates/empack-tests/**"
  - "crates/empack/src/**"
  - "scripts/**"

comment:
  layout: "condensed_header, diff, condensed_files, condensed_footer"
  behavior: default
  require_changes: false
